doctype html
html
    head
        meta(charset='UTF-8')
        link(rel='stylesheet', href='../../src/css/common.css')
        block head-add
    body
        header(class='flex-grid')
            img(id='logo', alt='logo')
            include nav.pug
            a(id='cart', class="cart blue-text", href="#") В корзине
        block page
            section(id="main")
        footer(class='flex-grid')
            section(class='copyright') Company name — 2016
            include nav.pug
    block scripts
